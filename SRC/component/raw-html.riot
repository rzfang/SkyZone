<raw-html>
  <pre />
  <style>
    :host>pre { white-space: collapse; }
  </style>
  <script>
    export default {
      onBeforeMount (props) {
        this.root.innerHTML = `<pre>${props.html}</pre>`;
      },

      onMounted (props) {
        this.OnBrowser(() => {
          this.$('pre').innerHTML = this.props.html;
        });
      },

      onUpdated () {
        this.OnBrowser(() => {
          this.$('pre').innerHTML = this.props.html;
        });
      },
    };
  </script>
</raw-html>
